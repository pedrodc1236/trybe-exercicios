/* 4. Faça um programa que retorne o maior de três números. 
    Defina no começo do programa três constantes com os valores que serão comparados. */
    
    const positiveOrNegative = -10;

    if (positiveOrNegative > 0) {
        console.log("O numéro " + positiveOrNegative + " é POSITIVO!")
    } else if (positiveOrNegative < 0) {
        console.log("O número " + positiveOrNegative + " é NEGATIVO!")
    } else {
        console.log(positiveOrNegative)
    }
 
